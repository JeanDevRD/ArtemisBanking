@model ArtemisBanking.Core.Application.Dtos.SavingsAccount.SavingsAccountDto
@{
    ViewData["Title"] = "Realizar Transferencia";
}

<div class="container mt-4 col-md-6">
    <h3>Transferencia desde Cuenta @ViewBag.CuentaNumero</h3>
    
    <div class="alert alert-info">
        <strong>Saldo Disponible:</strong> @ViewBag.SaldoDisponible?.ToString("C2")
    </div>

    <div class="card shadow-sm">
        <div class="card-body">
            <form asp-controller="Transferencias" asp-action="Index" method="post" class="bg-light p-4 rounded">
                <input type="hidden" name="cuentaId" value="@ViewBag.CuentaId" />

                <div class="form-group mb-3">
                    <label>Beneficiario</label>
                    <select name="beneficiarioId" class="form-control" required>
                        <option value="">-- Seleccione un beneficiario --</option>
                        @if (ViewBag.Beneficiarios != null)
                        {
                            @foreach (var b in ViewBag.Beneficiarios)
                            {
                                <option value="@b.Id">@b.FirstName @b.LastName (@b.AccountNumber)</option>
                            }
                        }
                    </select>
                </div>

                <div class="form-group mb-3">
                    <label>Monto a Transferir</label>
                    <input type="number" name="monto" class="form-control" step="0.01" min="0.01" required placeholder="0.00" />
                </div>

                <div class="form-group mb-3">
                    <label>Concepto (Opcional)</label>
                    <textarea name="concepto" class="form-control" rows="3" placeholder="Descripción de la transferencia"></textarea>
                </div>

                <button type="submit" class="btn btn-success w-100">Continuar</button>
                <a asp-controller="Cuentas" asp-action="Detalle" asp-route-id="@ViewBag.CuentaId" class="btn btn-outline-secondary w-100 mt-2">Cancelar</a>
            </form>
        </div>
    </div>
</div>
