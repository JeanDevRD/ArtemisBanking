@model IPagedList<ArtemisBankingWebApp.Models.ProductViewModel>
@{
    ViewData["Title"] = "Mis Productos";
}
<div class="container-fluid mt-4">
    <h2 class="mb-4">Mis Productos Financieros</h2>

    <div class="row text-center mb-4">
        <div class="col-md-4">
            <div class="card shadow-sm bg-success text-white">
                <div class="card-body">
                    <h5>Saldo Total</h5>
                    <h3>@ViewBag.TotalBalance</h3>
                </div>
            </div>
        </div>
        <div class="col-md-4">
            <div class="card shadow-sm bg-danger text-white">
                <div class="card-body">
                    <h5>Deuda Total</h5>
                    <h3>@ViewBag.TotalDebt</h3>
                </div>
            </div>
        </div>
        <div class="col-md-4">
            <div class="card shadow-sm bg-warning text-dark">
                <div class="card-body">
                    <h5>Próximo Pago</h5>
                    <h3>@ViewBag.NextPayment</h3>
                </div>
            </div>
        </div>
    </div>

    <table class="table table-hover shadow-sm">
        <thead class="table-dark">
            <tr>
                <th>Tipo</th>
                <th>Número</th>
                <th>Saldo / Monto</th>
                <th>Estado</th>
                <th>Acciones</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var p in Model)
            {
                <tr>
                    <td>@p.Type</td>
                    <td>@p.Number</td>
                    <td>@p.Amount.ToString("C2")</td>
                    <td><span class="badge bg-@(p.Status == "Active" || p.Status == "Active" ? "success" : "secondary")">@p.Status</span></td>
                    <td>
                        <a asp-controller="@(p.TypeId == 1 ? "Accounts" : p.TypeId == 2 ? "Loans" : "Cards")"
                           asp-action="Detail" asp-route-id="@p.Id" class="btn btn-sm btn-info">Ver</a>
                        <a asp-controller="@(p.TypeId == 1 ? "Transfers" : "Payments")"
                           asp-action="@(p.TypeId == 1 ? "Index" : p.TypeId == 2 ? "PayInstallment" : "PayCard")"
                           asp-route-id="@p.Id" class="btn btn-sm btn-primary">Operar</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>

    <div class="d-flex justify-content-center">
        @Html.PagedListPager(Model, page => Url.Action("Index", new { page }),
        new PagedListRenderOptions { LiElementClasses = new[] { "page-item" }, PageClasses = new[] { "page-link" } })
    </div>
</div>
